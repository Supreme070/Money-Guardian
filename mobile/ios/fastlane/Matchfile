# Fastlane Match configuration for Money Guardian
# See: https://docs.fastlane.tools/actions/match/

# SETUP REQUIRED:
# 1. Create a private GitHub repo for certificates (e.g., your-org/fastlane-certs)
# 2. Run `fastlane match appstore` locally to generate and store certs
# 3. Add MATCH_PASSWORD and MATCH_GIT_AUTH secrets to GitHub repo settings

git_url("https://github.com/Supreme070/fastlane-certs")
storage_mode("git")
type("appstore")
app_identifier("com.moneyguardian.app")
